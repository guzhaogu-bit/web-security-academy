# Web Cache Deception Web缓存欺骗 实验全套整理（超清晰版）
## 一、核心概念总览
**Web缓存欺骗**本质：**缓存服务器 和 源站服务器 对URL解析规则不一致**
利用差异让：
- 源站：访问**动态敏感页面**（个人中心/账号API Key）
- 缓存：当成**静态文件/静态目录资源**缓存
攻击者直接访问缓存链接，拿到敏感数据

## 二、四大漏洞类型（考试/实验全考点）
### 1. 普通分隔符差异 Delimiter discrepancies
- 原理：源站把某字符当**分隔符**截断路径；缓存不当成分隔符
- 常见分隔符：`; ? . %00`
- 例子：
  `/user;test.css`
  - 源站：识别 `;` → 只解析 `/user`
  - 缓存：看成完整 `.css` 静态文件 → 缓存敏感页面

### 2. 分隔符解码差异 Delimiter decoding discrepancies
- 原理：双方**对URL编码字符解码时机/规则不同**
- 典型：`%23(#)`、`%3f(?)`
- 例子：
  `/myaccount%3fwcd.css`
  - 缓存：按编码URL匹配 `.css` 缓存
  - 源站：解码 `%3f` 为 `?` 当分隔符，只解析 `/myaccount`

### 3. 编码不可打印字符利用
重点测试：
- `%00` 空字节
- `%0A` 换行
- `%09` 制表符
特点：被服务器解码后可**截断URL路径**，充当隐形分隔符。

### 4. 规范化差异 Normalization discrepancies + 静态目录缓存规则
- 结合 **路径遍历 + URL编码 + 目录前缀缓存**
- 缓存规则常匹配：`/static` `/assets` `/images` `/scripts`
- 经典载荷：
  `/static/..%2fprofile`
  - `%2f` 是编码 `/`
  - 源站：解码 + 解析 `..` → 跳转到 `/profile`
  - 缓存：不解码不解析 `..`，只看开头 `/static` 就缓存

---

## 三、通用测试流程（所有实验通用步骤）
### 步骤1：找基准页面
1. 访问正常接口：`/my-account`
2. 随便加后缀测：`/my-accountabc` → 404，作为参照

### 步骤2：Burp Intruder 爆破分隔符
1. 载荷位置：`/my-account§§abc`
2. Payload列表：
```
; ? . # %00 %0A %09 %23 %3f
```
3. **关键配置**：关闭 Payload encoding 自动URL编码
4. 看状态码：
   - 返回 **200** = 源站认可为**分隔符**
   - 返回 404 = 不是分隔符

### 步骤3：校验缓存是否识别该分隔符
构造：`/my-account;xxx.js`
- 第一次请求：`X-Cache: miss`
- 第二次请求：`X-Cache: hit`
→ 说明**缓存不识别 ;**，可以利用

### 步骤4：构造攻击链接 + 诱导受害者访问
1. 构造 payload：`/my-account;随机字符.js`
2. 用漏洞服务器植入脚本诱导 carlos 访问
3. 受害者访问后，个人信息被缓存
4. 你直接访问该链接，读取缓存里的 API Key

---

## 四、关键注意事项（避坑）
1. 浏览器会自动解析 `../`、`#`，**必须对遍历字符做URL编码**
2. Burp 测试一定要**关闭自动编码**，否则分隔符被转义测不出来
3. 不要用固定随机串，要换唯一字符串，防止命中自己的缓存
4. 缓存规则两类：
   - 按**文件后缀** `.css/.js/.ico`
   - 按**目录前缀** `/static` 等

---

## 五、一句话极简记忆
1. **普通分隔符**：源站截断，缓存认后缀
2. **解码差异**：缓存看编码串，源站先解码再截断
3. **规范化+路径遍历**：缓存认/static前缀，源站解码..跳敏感页面
4. 套路统一：测分隔符 → 测缓存差异 → 造链接 → 引受害者 → 读缓存拿数据

需要我给你整理一份**可直接复制的Burp载荷列表 + 实验通用Payload模板**吗？直接粘贴就能用。
