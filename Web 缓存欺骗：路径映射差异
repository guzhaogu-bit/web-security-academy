# Web缓存欺骗：路径映射差异 实验完整总结
## 一、实验原理
### 核心漏洞根源
**源服务器**和**CDN缓存服务器**对URL路径的解析规则**不一致**：
1. **源服务器（后端）**
采用REST风格路径匹配，只识别核心接口 `/my-account`，**忽略后面附加的任意路径片段**，访问 `/my-account/xxx.js` 依然返回个人账号敏感数据（API密钥）。

2. **缓存服务器（CDN）**
采用传统文件路径匹配，**把完整URL当成真实文件路径**；
只要URL以 `.js/.css` 等静态后缀结尾，就会按静态资源规则**缓存页面响应**。

### 攻击逻辑
1. 构造 `域名/my-account/wcd.js`
2. 后端正常返回用户个人信息（带API密钥）
3. 缓存服务器把这个动态页面当成JS静态文件缓存
4. 诱导受害者访问该链接，**受害者的隐私数据被存入缓存**
5. 攻击者直接访问同一条URL，读取缓存里受害者的API密钥

---

## 二、详细操作步骤（可复盘）
1. **登录测试账号**
Burp内置浏览器登录：`wiener : peter`，进入个人中心看到自己的API密钥，确定 `/my-account` 是敏感接口。

2. **测试路径映射规则**
把 `/my-account` 改 `/my-account/abc`，发包仍返回API密钥，证明后端忽略后缀路径。

3. **测试缓存规则**
改成 `/my-account/abc.js`：
- 第一次请求：`X-Cache: miss` 未命中缓存
- 30秒内重发：`X-Cache: hit` 命中缓存
证明 `.js` 后缀会被CDN缓存。

4. **构造恶意跳转链接**
```html
<script>document.location="https://0a59001c03ab700780d826cc004e00d9.web-security-academy.net/my-account/wcd.js"</script>
```
放到漏洞服务器Body，投送给受害者。

5. **读取缓存窃取密钥**
自己浏览器访问：
`https://0a59001c03ab700780d826cc004e00d9.web-security-academy.net/my-account/wcd.js`
拿到carlos的API密钥，提交通关。

---

## 三、实验经验总结 & 技巧
### 1. 漏洞识别技巧
- 只要接口**加任意后缀路径仍正常返回数据**，大概率存在路径映射差异；
- 看响应头 `X-Cache`（hit/miss）、`Cache-Control: max-age` 判断是否可缓存。

### 2. 攻击固定套路
1. 找含敏感信息的动态接口（个人中心、用户资料等）
2. 加任意路径片段，测试后端是否忽略多余路径
3. 加 `.js/.css/.ico` 静态后缀，测试CDN是否缓存
4. 构造跳转链接诱导受害者访问
5. 攻击者访问同链接读取缓存敏感数据

### 3. 避坑要点
- 不要用自己测试过的 `abc.js`，换全新名字 `wcd.js`，避免读到自己的缓存；
- 必须在 `max-age` 有效期内重发请求，验证缓存是否生效；
- 不能直接浏览器测试缓存，要用Burp Repeater看响应头。

### 4. 防御思路
- 源服务器对动态接口禁止以静态后缀结尾的URL访问；
- CDN配置缓存规则，不对用户个人中心、动态接口做缓存；
- 校验URL完整路径，不允许任意拼接路径后缀。
